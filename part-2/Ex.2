# docker-compose.yml

version: '3.8'

services:

    simple:
      image: devopsdockeruh/simple-web-service
      ports:
        - 8080:8080
      build: .
      command: server
      container_name: simple
